# Talos GCP Deployment Configuration
# Usage: ./talos-gcp -c talos.env deploy

# --- Cluster Identity ---
# Unique name for the cluster (used for resource naming)
CLUSTER_NAME="talos-dev-cluster"

# GCP Project ID (Defaults to gcloud config get-value project)
# PROJECT_ID="my-gcp-project-id"

# Region and Zone
REGION="us-central1"
ZONE="us-central1-b"

# --- Compute Resources ---
# Machine Types (See: gcloud compute machine-types list)
CP_MACHINE_TYPE="e2-standard-2"
WORKER_MACHINE_TYPE="e2-standard-2"

# Disk Size (GB)
CP_DISK_SIZE="200GB"
WORKER_DISK_SIZE="200GB"

# Node Counts
CP_COUNT=3
WORKER_COUNT=2

# --- Networking ---
# VPC Configuration
VPC_NAME="talos-vpc"
SUBNET_RANGE="10.0.0.0/24"

# Ingress Configuration (Semicolon separated port groups)
# Each group gets a dedicated External IP and Forwarding Rule
# Example: "80,443;8080" creates:
#   - IP 1 -> Ports 80, 443
#   - IP 2 -> Port 8080
INGRESS_IPV4_CONFIG="80,443"

# --- Advanced ---
# Talos Version
TALOS_VERSION="v1.12.3"
# Kubectl Version
KUBECTL_VERSION="v1.35.0"

# Bastion Host Image
BASTION_IMAGE_FAMILY="ubuntu-2204-lts"
BASTION_IMAGE_PROJECT="ubuntu-os-cloud"

# Resource Labels (Comma separated key=value)
LABELS="managed-by=talos-script,environment=dev"
