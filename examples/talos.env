# Talos GCP Deployment Configuration
# Usage: ./talos-gcp -c talos.env deploy

# --- Cluster Identity ---
# Unique name for the cluster (used for resource naming)
CLUSTER_NAME="talos-dev-cluster"

# GCP Project ID (Defaults to gcloud config get-value project)
# PROJECT_ID="my-gcp-project-id"

# Region and Zone
REGION="us-central1"
ZONE="us-central1-b"

# --- Compute Resources ---
# Machine Types (See: gcloud compute machine-types list)
CP_MACHINE_TYPE="e2-standard-2"
WORKER_MACHINE_TYPE="e2-standard-2"

# Disk Size (GB)
CP_DISK_SIZE="200GB"
WORKER_DISK_SIZE="200GB"

# Node Counts
CP_COUNT=3
WORKER_COUNT=2

# Additional Worker Disks (Format: type:size[:device-name] space-separated)
# WORKER_ADDITIONAL_DISKS="pd-ssd:100GB:fast-data pd-standard:500GB:slow-data"

# --- Networking ---
# VPC Configuration
VPC_NAME="talos-vpc"
SUBNET_RANGE="10.0.0.0/24"

# Ingress Configuration
# Number of Static External IPs to reserve (Default: 1)
INGRESS_IP_COUNT=1

# --- Advanced ---
# Talos Version (Global)
TALOS_VERSION="v1.12.3"

# Mixed Role Architecture (Override Defaults)
# CP_TALOS_VERSION="v1.12.3"
# WORKER_TALOS_VERSION="v1.12.3"
# CP_EXTENSIONS="siderolabs/gvisor"
# WORKER_EXTENSIONS="siderolabs/nvidia-container-toolkit"

# Kubectl Version
KUBECTL_VERSION="v1.35.0"

# Bastion Host Image
BASTION_IMAGE_FAMILY="ubuntu-2404-lts-amd64"
BASTION_IMAGE_PROJECT="ubuntu-os-cloud"

# Resource Labels (Comma separated key=value)
LABELS="managed-by=talos-script,environment=dev"
